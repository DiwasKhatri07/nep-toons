# NepToons GitHub Pages preview

This directory is a static UI preview for GitHub Pages. It supports browsing, search, demo profiles, and browser-local favorites.

The full Flask application remains in `test.py` and is required for authentication, real profiles, movie add/remove, database writes, token updates, and HLS streaming.
